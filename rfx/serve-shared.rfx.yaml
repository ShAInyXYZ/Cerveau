rfx: 1
name: serve-shared
description: Restart the model server on the shared profile (~73 tok/s, ~12GB VRAM free)
risk: dangerous
modes: [autopilot]
kind: pipeline
card: {fs: [workspace], network: any, subprocess: true}
contract: {max_ms: 30000}
steps:
  - bash: CRV_PROFILE=shared ~/.crv/bin/serve-model.sh
