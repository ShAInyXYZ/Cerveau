# Handover — 2026-09-05

Branch `feat/stepwise-runs`, HEAD `510df03`. Nothing has been pushed since `f3fb353` (2026-08-20):
`main` carries 28 unpushed commits (panel/android/security polish, not covered here) and this
branch adds 30 on top of `main`. Working tree is clean except two untracked files written by a
separate Codex audit session (see the end).

## What the session set out to do

1. Make the W8A16 Core stable (the user's stated priority: "stability and robustness, not
   tokens per second"), document its engine patches, give each Core profile its own DGV.
2. Make **the plan the execution unit**: after a plan is committed, every step gets its own
   prompt, its own run, its own check; only a passing check advances; a later step that finds an
   earlier one incomplete reopens it as a revision. Native chat strip and the RFX Planner both
   drive the same core. "Not decoration — a proper plan showing up and the loop working to it."
3. Keep the chat honest: the working log survives the turn, the thinking knobs stop colliding.

## Stack state right now

| Thing | State |
|---|---|
| `crv` installed | `~/.crv/bin/crv`, built from `510df03`, `cerveau.service` active on :7700 |
| Core | Qwen3.8-27B W8A16 TP=4 (`vllm-27b-w8a16`), prefix caching ON, `GPU_UTIL=0.92` via `~/.config/cerveau/cores.d/vllm-27b-w8a16.env` |
| vLLM patches | 9 files in `~/qwen-serving/patches/`, applied in site-packages; listed in `deploy/profiles/ENGINE-PATCHES.md`. A venv reinstall silently removes them. |
| Thinking | mode `autopilot` (thinks on plan + every step), effort `medium` |
| Sampling | preset `default` (sends no temperature/top_p; model's generation_config applies) |
| Planner pack | `~/.crv/rfx/planner/ui/panel.html` — edited this session, **unversioned** |

## Commits on this branch, oldest first, and what each pretended to do

### Core / engine
- `c811592` chore: ignore `Benchmark/` (benchmark outputs kept on disk, not source).
- `5fdfc00` feat(llm): thinking is a level (off/low/medium/xhigh) per call or per mode, with
  overflow step-down; adds the `default` sampling preset. **Verified live.**
- `8d8ba53` feat(cores): installable Core profiles (`deploy/profiles/*`), `GET/PUT
  /api/cores/{id}/params` writing a diff to `cores.d/<id>.env`; ENGINE-PATCHES.md. **Verified.**
- `dd1886a` feat(idle): park the Core when idle; panel idle screen.
- `5e23578` feat(window): probe the Core's real serving window from the engine.
- `cc10624` feat(api): routes for thinking, Core switch, Core params, idle.
- `b4ceb06` feat(panel): THINK pill, Engine settings, idle screen; first half of the plan-strip fix.

### Plan gate (getting a real plan out of the model)
- `48868c2` feat(plan): salvage a plan written as prose/markdown into a plan event. Still there
  as fallback; superseded in practice by forced tool_choice.
- `ca91565` fix(plan): the six bugs that made the gate a no-op — `commit_plan` was fenced to
  discussion (empty tool list in autopilot); read-before-plan (orientation free, 4 reads) then
  `tool_choice` FORCES `commit_plan`; session id on ctx instead of the shared global
  `SessionContext`; hand-off to the supervisor; step runner uses the session registry; an overrun
  step still runs its check. **Verified live.**
- `75ea1fc` fix(loop): an answer that needs no plan ends the turn (`answersWithoutAPlan`).
  **Verified live** ("president in 1950", "when was Porsche founded").
- `714b5bf` feat(plan): planning reads ride into every step (`planningSources`, 40k cap);
  `steps`/`files` required, minItems 1; 3 commit tries; segment-wise existence rule.
  **First clean run: 11/11 steps, 7 min, session `improve-rejects3`.**

### Stepwise execution
- `3a8db31` feat(loop): `plan.Verify` (eval / command / contains, must be able to fail),
  `RunVerify` (fails closed), `Supervisor` (cursor, retry once, revisions cap 2, downstream
  re-verify, pair-fail standoff), `StepPrompt`. Stub-tested.
- `612832b` feat(loop): wires the supervisor into `RunAutopilot`; checkpoints per run.
- `f1342de` fix(report): checkpoints bound to steps by `index`, not title.
- `890413c` feat(api): `GET /api/sessions/{id}/plan`, `POST …/plan/step` (`step:-1` = next;
  rerun of a passed step = revision). One path `runPlanFrom` for autopilot and buttons.
- `6b5a973` feat(rfx): `rfx.plan()` / `rfx.runStep()`; planner pack stopped composing prose.
- `52eed21` fix(loop): repeat guard resets a signature when the workspace fingerprint moved;
  steps honour the THINK level (they ran with zero reasoning before).
- `12b97be` feat(loop): an autopilot turn with an unfinished plan resumes it via the supervisor
  ("keep going" used to start a free turn and replay the stuck reply).
- `d27b675` fix(loop): a retry's prompt carries the failed verdict and evidence; per-step
  iteration cap replaced by guard-based extension while the workspace keeps changing.
  **Not yet exercised live** — see NFQ below.

### Guards, tools, report, panel
- `bd00c30` fix(loop): a new successful tool result clears the stall counter; cached repeats do
  not; thresholds 4/8.
- `a8ceff7` fix(report): no disk reconciliation when steps share a file set (false 4/4 done).
- `14bc01c` fix(check_page): eval over the served URL so ES-module pages can be probed.
- `29d1e35` fix(tools): apply_patch says which anchor drifted; descriptions say what a tool cannot do.
- `0aa0a93` fix(panel): keep the last good report on a 404 (second half of the strip fix).
- `1fd6b3f` fix(panel): escape inline code before DOMPurify (table cells were vanishing).
- `384de4d` feat(chat): `episodic.Fold` keeps tool/note/error events per reply
  (`State.Logs`), served by `/state`; `TurnLog.svelte` shows them collapsed above each reply.
- `06b4f74` fix(panel): thinking-mode labels are never / plan only / every step / every turn;
  the pill shows scope next to effort. API values unchanged.

### DGV
- `15c662a` six diagrams (Architecture, profile-w8a16-tp4, profile-bf16-tp4, run-dataflow,
  run-stepwise, + Architecture update). `e9a1426`, `510df03` keep `run-stepwise` current.
  All flags resolved; lint clean.

## What is proven live vs only stub-tested

Proven on the real Core: plan gate end to end, forced commit, 11/11 stepwise run, answer-without-
plan, check_page eval over http, prefix caching byte-identical after the vllm#48375 patch.

Only stub-tested so far: retry-with-evidence and iteration extension (`d27b675`), resume of an
unfinished plan from a chat turn (`12b97be`), the per-reply log rendering (server side verified
on the NFQ session: 7 replies carry logs; the UI was built, not eyeballed).

## Open work, in order

1. **NFQ benchmark session** `20260905-002618-nfq` (workspace `~/Pictures/MultiRigGames/NFQ/`):
   steps 1–3 passed, step 4 failed (Three.js `TypeError … reading 'x'`, 2128× in console),
   step 5 pending. Typing "keep going" in that session now retries step 4 with the error in its
   prompt. This is the first live test of `d27b675`. Watch the "what happened" toggle too.
2. **Native plan strip** still polls `/report`; it should read `/api/sessions/{id}/plan` like the
   planner does (DGV `run-stepwise` node `strip`, status todo).
3. **Planner pack** is unversioned in `~/.crv/rfx/planner/`. Decide where it lives in the repo.
4. **Push.** Neither `main` nor this branch has left the machine since 2026-08-20.
5. **Codex audit (untracked, not mine):** `docs/loop-audit-2026-09-05.md` and
   `dgv/loop-audit.dgv.json`, written 04:25 by another session against `510df03`. Finding L01
   (a session can have overlapping runs, `internal/loop/run.go:45`) looks real. Read before
   committing or discarding; do not assume it is finished.
6. Stray data from pre-fix runs, user's call: session `20260904-230026-ccar` holds two plans that
   are not its own (`evt_000097`, `evt_000098`); a 445-byte `js/config.js` was written outside a
   session workspace and never located.

## How to work on it

```
cd ~/Documents/GitHub/Cerveau
(cd panel && npm run build)          # panel → internal/panel/dist
go build -o /tmp/crv ./cmd/crv && go test ./...
systemctl --user stop cerveau.service; cp /tmp/crv ~/.crv/bin/crv; systemctl --user start cerveau.service
curl -s :7700/api/health
curl -s :7700/api/thinking
curl -s :7700/api/sessions/<id>/plan          # cursor + per-step verdicts
curl -s :7700/api/sessions/<id>/state | jq '.logs | keys'
```

Read `dgv/run-stepwise.dgv.json` (viewer :7710) before touching `internal/loop`. Memory notes:
`cerveau-m9-checkpoint`, `w8a16-core-tuning`, `cerveau-dgv-diagrams` under the project memory dir.

## Working with this user

Short answers, one phrase when asked. Never claim something works without running it. Plan in
DGV first for structural changes, flag issues there, resolve them when fixed. Commit step by
step. Never `mv`/`rm` data without a verified copy first.
