# Embedder placement for this profile. The four 3090s are the Core's; the
# 3060 (index 4 — verify with `nvidia-smi -L`) takes the embedder, so recall
# stops competing with the Core for CPU and runs at GPU speed.
EMBED_DEVICE=cuda
CUDA_VISIBLE_DEVICES=4
EMBED_THREADS=4
